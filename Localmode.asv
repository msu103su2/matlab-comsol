function result = Localmode(Eigenfreq)
%try simulated anneling
Ti = 300;
Tf = 2;
r = 0.9;
T = Ti;
while T > Tf
   %generate new parameters
   delta = new - old;
   if delta < 0 or exp(?delta/T) > rnd(0,1);
        distance= distance + delta;
        path = new path
   end
   T = r*T
end
end